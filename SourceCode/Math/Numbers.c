#include<stdio.h>
#include<math.h>

int sum(int x,int y)
{
  return x+y;
}
//added to testing branch
int mulitply(int x,int y)
{
  return x*y;
}

int main()
{
	printf("%d",sum(2,3));
}
